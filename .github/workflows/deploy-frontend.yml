name: Deploy Frontend

on:
  push:
    branches: [main]
    paths: 
      - 'src/**'
      - 'package.json'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'
          
      - name: Install
        run: npm ci
        
      - name: Build
        run: npm run build
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          TENCENT_SECRET_ID: ${{ secrets.TENCENT_SECRET_ID }}
          TENCENT_SECRET_KEY: ${{ secrets.TENCENT_SECRET_KEY }}
          TENCENT_BUCKET: ${{ secrets.TENCENT_BUCKET }}
          TENCENT_REGION: ${{ secrets.TENCENT_REGION }}
          GCP_CREDENTIALS_PART1: ${{ secrets.GCP_CREDENTIALS_PART1 }}
          GCP_CREDENTIALS_PART2: ${{ secrets.GCP_CREDENTIALS_PART2 }}
          GCP_CREDENTIALS_PART3: ${{ secrets.GCP_CREDENTIALS_PART3 }}
          GCP_CREDENTIALS_PART4: ${{ secrets.GCP_CREDENTIALS_PART4 }}
          
      # EdgeOne CLI 배포는 복잡하므로 콘솔 연동 예정
      # 여기서는 빌드 성공 확인까지만 진행할 예정